/**
 * Заглушка слота модалки. Расширение обычное, а не .server.tsx: слот должен
 * существовать в обеих сборках, иначе layout не может его разрешить и падает.
 * В демо-сборке перехватывающего маршрута рядом нет, и слот всегда пуст.
 */
export default function ModalDefault() {
  return null
}
